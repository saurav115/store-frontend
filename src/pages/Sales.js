import React from 'react';
import { Box, Typography } from '@mui/material';

const Sales = () => {
  return (
    <Box>
      <Typography variant="h5" gutterBottom>
        Sales
      </Typography>
      <Typography variant="body1">
        This is the sales page where sales data will be displayed.
      </Typography>
    </Box>
  );
};

export default Sales;
